# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/src/generated/prisma

# local-disk fallback for gallery uploads (dev only — production uses Vercel Blob)
/public/uploads/gallery/*
!/public/uploads/gallery/.gitkeep

# agent skill docs auto-installed by `prisma init` (reference material, not app code)
/.agents/
/.claude/
/.windsurf/
skills-lock.json
